{
  "name": "project_d_12",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun server/index.ts",
    "dev": "bun --watch server/index.ts",
    "build": "bun build server/index.ts --outdir dist",
    "deploy": "wrangler deploy --minify --name project_d_12 dist/index.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.2.2",
    "hono": "^4.5.4",
    "zod": "^3.23.8"
  }
}
